apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "generation.fullname" . }}-test-connection"
  labels:
    {{- include "generation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "generation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
